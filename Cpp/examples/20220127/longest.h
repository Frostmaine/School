// function to find the longest string
// pre-conditons: the arguments have valid strings in them
// post-conditions: returns the string that is as long as or
// longer than the other strings
#include <string>
std::string longest(std::string, std::string, std::string);
